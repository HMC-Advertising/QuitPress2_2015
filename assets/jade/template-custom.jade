- /* Template Name: Custom Template */ 
- get_header(); 
- if ( have_posts() ) : while ( have_posts() ) : the_post(); 
article(role="main")
	h3(class="page-title")
		- the_title(); 
	- the_content();  
- endwhile; endif; 
- get_sidebar(); 
- get_footer(); 